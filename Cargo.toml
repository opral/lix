[workspace]
resolver = "3"
members = [
  "packages/*",
  "plugins/*",
  # This patched dependency has private parser-equivalence tests that must run
  # with the workspace suite while the upstream optimization is evaluated.
  "vendor/markdown-syntax",
]

# Reference baselines that must never join the workspace build graph. They are
# committed so the numbers stay reproducible from a clean checkout, and
# excluded so no CI lane pays to compile them. Run them explicitly from their
# own directory.
exclude = ["benchmarks/sqlite_scan_baseline"]

[workspace.package]
version = "0.11.0"
authors = ["Opral"]
edition = "2024"
description = "Embeddable version control for apps and AI agents."
license = "MIT"
homepage = "https://lix.dev"
repository = "https://github.com/opral/lix"
documentation = "https://docs.rs/lix"
readme = "README.md"
keywords = ["version-control", "ai-agents", "semantic-diff", "change-tracking", "embeddable"]
categories = ["development-tools", "artificial-intelligence", "filesystem"]
rust-version = "1.93"

[workspace.dependencies]
lix_cli = { path = "packages/cli", version = "0.11.0" }
lix = { path = "packages/lix", version = "0.11.0", default-features = false }
lix_storage_filesystem = { package = "lix-storage-filesystem", path = "packages/storage-filesystem", version = "0.1.0", default-features = false }
lix_storage_rocksdb = { package = "lix-storage-rocksdb", path = "packages/storage-rocksdb", version = "0.1.0" }
lix_storage_slatedb = { package = "lix-storage-slatedb", path = "packages/storage-slatedb", version = "0.1.0" }
lix_js_sdk = { path = "packages/js-sdk", version = "0.11.0" }
mimalloc = { version = "0.1.52", features = ["local_dynamic_tls"] }

[workspace.lints.rust]
missing_copy_implementations = "warn"
missing_debug_implementations = "warn"
trivial_casts = "warn"
trivial_numeric_casts = "warn"
unexpected_cfgs = { level = "warn", check-cfg = [] }
unused_import_braces = "warn"
unused_qualifications = "warn"
# unused_crate_dependencies = "warn"

unconditional_recursion = "deny"

[workspace.lints.clippy]
# https://rust-lang.github.io/rust-clippy/master/index.html
all = { level = "warn", priority = -2 }
cargo = { level = "warn", priority = -1 }
complexity = { level = "allow", priority = -1 }
correctness = { level = "warn", priority = -1 }
nursery = { level = "allow", priority = -1 }
pedantic = { level = "allow", priority = -1 }
perf = { level = "warn", priority = -1 }
restriction = { level = "allow", priority = -1 }
style = { level = "allow", priority = -1 }
suspicious = { level = "warn", priority = -1 }

# Boxing hot-path batch/actor enum variants adds heap allocations, including
# per-file allocations, which is worse than their larger stack discriminants.
large_enum_variant = "allow"

# Cargo metadata/dependency topology is validated elsewhere in the release
# pipeline and does not describe runtime correctness or performance.
cargo_common_metadata = "allow"
redundant_feature_names = "allow"
multiple_crate_versions = "allow"

# Keep the published 0.2.0 surface local while the Markdown parser prepass
# optimization is evaluated. The vendored crate intentionally contains only
# its published library sources and licensing/readme material; remove this
# override once the equivalent upstream release is adopted.
[patch.crates-io]
markdown-syntax = { path = "vendor/markdown-syntax" }
