{
  "$schema": "https://lix.dev/schema-v1.json",
  "key": "lix_key_value",
  "description": "A repository setting or application value stored under a key.",
  "columns": [
    {
      "name": "key",
      "type": "text",
      "nullable": false,
      "description": "Unique key naming the value."
    },
    {
      "name": "value",
      "type": "jsonb",
      "nullable": true,
      "description": "Arbitrary JSON value. This field stays in the JSON domain even when different rows hold different JSON kinds."
    }
  ],
  "primary_key": [
    "key"
  ]
}
