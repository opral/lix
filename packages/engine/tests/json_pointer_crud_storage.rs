#![cfg(feature = "storage-benches")]

#[test]
fn json_pointer_crud_storage_placeholder() {
    // The previous storage-bench comparison test used the removed storage v1
    // backend API and old bench-local backend fixtures. The promoted
    // storage-benches feature now builds the storage_v2 bench target directly.
}
