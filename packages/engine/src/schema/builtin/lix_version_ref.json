{
  "x-lix-key": "lix_version_ref",
  "x-lix-version": "1",
  "description": "Version head pointer. This row records which commit a version should currently resolve to in the local runtime. It is intentionally not part of canonical commit membership, because refs may be reset client-side after sync to the resolved head without introducing content conflicts.",
  "x-lix-primary-key": [
    "/id"
  ],
  "x-lix-foreign-keys": [
    {
      "properties": [
        "/commit_id"
      ],
      "references": {
        "schemaKey": "lix_commit",
        "properties": [
          "/id"
        ]
      }
    }
  ],
  "x-lix-override-lixcols": {
    "lixcol_file_id": "\"lix\"",
    "lixcol_plugin_key": "\"lix\"",
    "lixcol_global": "true",
    "lixcol_untracked": "true"
  },
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "x-lix-default": "lix_uuid_v7()",
      "description": "Version identifier whose head pointer is being stored."
    },
    "commit_id": {
      "type": "string",
      "description": "Commit that the version should currently resolve to in the local runtime."
    }
  },
  "required": [
    "id",
    "commit_id"
  ],
  "additionalProperties": false
}
