{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "key",
    "runtime",
    "api_version",
    "match",
    "entry",
    "schemas"
  ],
  "properties": {
    "key": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z][a-z0-9_-]*$"
    },
    "runtime": {
      "type": "string",
      "const": "wasm-component-v2"
    },
    "api_version": {
      "type": "string",
      "const": "2.0.0"
    },
    "match": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path_glob"],
      "properties": {
        "path_glob": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1024
        },
        "content_type": {
          "type": "string",
          "enum": ["text", "binary"]
        }
      }
    },
    "entry": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512
    },
    "schemas": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 512
      }
    }
  }
}
