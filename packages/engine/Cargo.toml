[package]
name = "lix_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
sqlx = { version = "0.8", features = ["sqlite", "postgres", "runtime-tokio-rustls"] }
tokio = { version = "1", features = ["rt", "macros", "sync"] }

[dev-dependencies]
# Keep test runtime available
