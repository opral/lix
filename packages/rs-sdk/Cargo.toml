[package]
name = "lix_rs_sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
lix_engine = { path = "../engine", version = "0.1.0" }

async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
