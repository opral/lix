{
  "$schema": "https://lix.dev/schema-v1.json",
  "key": "csv_row",
  "description": "One decoded CSV record. Row order is controlled by order_key.",
  "columns": [
    {
      "name": "cells",
      "type": "jsonb",
      "nullable": false,
      "description": "Positional CSV fields as strings, including the first record. No header or number inference."
    },
    {
      "name": "id",
      "type": "uuid",
      "nullable": false,
      "default_expression": "uuidv7()"
    },
    {
      "name": "layout",
      "type": "jsonb",
      "nullable": true,
      "description": "Sparse lexical exceptions. Omit this object for canonical quoting and the table terminator."
    },
    {
      "name": "order_key",
      "type": "text",
      "nullable": false,
      "description": "File order: sort by order_key, then id. Nonempty lowercase hexadecimal, even length, not ending in 00."
    }
  ],
  "primary_key": [
    "id"
  ]
}
