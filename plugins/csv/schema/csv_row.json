{
  "x-lix-key": "csv_row",
  "x-lix-primary-key": [
    "/id"
  ],
  "description": "One decoded CSV/TSV record. Row order is controlled by order_key.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable row entity pk. New rows are generated as UUIDv7 strings. Must match the row entity_pk."
    },
    "order_key": {
      "type": "string",
      "pattern": "^(?:[0-9a-f]{2})*(?:0[1-9a-f]|[1-9a-f][0-9a-f])$",
      "description": "Opaque variable-length byte order key encoded as lowercase hex. Lexicographic string order matches row order."
    },
    "cells": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Decoded cell text."
      },
      "description": "Cell values for this row, in column order."
    }
  },
  "required": [
    "id",
    "order_key",
    "cells"
  ],
  "additionalProperties": false
}
