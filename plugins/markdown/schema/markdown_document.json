{
  "x-lix-key": "markdown_document",
  "x-lix-primary-key": [
    "/id"
  ],
  "description": "Projection root for a Markdown file. Markdown content and order are stored in markdown_block snapshots.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "const": "root",
      "description": "Stable root document id."
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
