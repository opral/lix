{
  "$schema": "https://lix.dev/schema-v1.json",
  "key": "excalidraw_element",
  "description": "One Excalidraw element keyed by its native stable ID. Numeric geometry is stored in native JSONB.",
  "columns": [
    {
      "name": "element_json",
      "type": "jsonb",
      "nullable": false,
      "description": "The complete JSON object, including numeric geometry and forward-compatible fields."
    },
    {
      "name": "id",
      "type": "text",
      "nullable": false,
      "description": "The native Excalidraw element ID."
    },
    {
      "name": "leading_json",
      "type": "text",
      "nullable": false,
      "description": "Exact whitespace before this array item; commas are supplied by the renderer."
    },
    {
      "name": "order_key",
      "type": "text",
      "nullable": false,
      "description": "A stable fractional key representing array order independently from identity."
    },
    {
      "name": "source_json",
      "type": "text",
      "nullable": true,
      "description": "Exact original JSON spelling. A formatting hint only; edited JSONB values remain authoritative."
    }
  ],
  "primary_key": [
    "id"
  ]
}
